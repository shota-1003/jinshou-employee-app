(function(){let drag=null,blockClick=false;document.addEventListener('pointerdown',e=>{const el=e.target.closest('.stock-matrix,.stock-overview');if(!el||e.pointerType!=='mouse'||e.button!==0)return;drag={el,x:e.clientX,y:e.clientY,left:el.scrollLeft,top:el.scrollTop,moved:false};blockClick=false});document.addEventListener('pointermove',e=>{if(!drag)return;const dx=e.clientX-drag.x,dy=e.clientY-drag.y;if(Math.abs(dx)>5||Math.abs(dy)>5)drag.moved=true;if(drag.moved){e.preventDefault();drag.el.classList.add('stock-dragging');drag.el.scrollLeft=drag.left-dx;drag.el.scrollTop=drag.top-dy}});function end(){if(!drag)return;blockClick=drag.moved;drag.el.classList.remove('stock-dragging');drag=null;setTimeout(()=>blockClick=false,0)}document.addEventListener('pointerup',end);document.addEventListener('pointercancel',end);window.addEventListener('blur',end);document.addEventListener('click',e=>{if(blockClick){e.preventDefault();e.stopImmediatePropagation();blockClick=false}},true)})();
